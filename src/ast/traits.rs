// use super::types::Type;

// #[derive(Debug, Clone, PartialEq)]
// pub enum Trait {
// 	Add(Type, Type),
// 	Sub(Type, Type),
// 	Mul(Type, Type),
// 	Div(Type, Type),
// 	Exp(Type, Type),

// 	Neg(Type),
// 	Call(Vec<Type>, Type),
// }

pub use super::types::Trait;